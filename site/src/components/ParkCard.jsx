import React from 'react';

function ParkCard(props) {
	const styles = {
		container: {
			border: '2px solid red',
		},
	};

	return <div style={styles.container}>ParkCard</div>;
}

export default ParkCard;
